import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const interpolationAni = () => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default interpolationAni

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }
})
